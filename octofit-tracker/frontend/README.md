# Octofit Tracker — Frontend

This frontend is initialized with React 19 and Vite (TypeScript).

Quick start:

1. cd octofit-tracker/frontend
2. npm install
3. npm run dev

What's included:
- Vite config (vite.config.ts)
- TypeScript config (tsconfig.json)
- Basic app skeleton (src/main.tsx, src/App.tsx)

Next steps (optional):
- Add routing (React Router)
- Add state management (Zustand, Redux, or Redux Toolkit)
- Add styling framework (Tailwind CSS already referenced in index.css)

(Generated by Copilot.)
