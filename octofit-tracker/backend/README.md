Backend notes: MongoDB

This backend includes Mongoose for MongoDB access.

Environment
- MONGO_URL: MongoDB connection string (defaults to mongodb://localhost:27017/octofit)

Files added
- src/db.ts — helper to connect to MongoDB using mongoose
- src/models/User.ts — example User model

Quick start:
1. cd octofit-tracker/backend
2. npm install
3. Set MONGO_URL in a .env file if not using the default
4. npm run dev

Next steps I can do for you:
- Scaffold src/index.ts to wire Express to the DB and add basic routes (health, users)
- Add migrations/seed scripts
- Add Dockerfile and docker-compose with a MongoDB service

(Generated by Copilot.)
